[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "clrinsights"
version = "0.1.0"

[tool.setuptools]
packages = ["clrinsights"]
package-dir = {"clrinsights" = "."}
